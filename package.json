{
  "name": "kiel-univis-athena-plugin",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.mjs --external:node:*",
    "test": "tsx --test tests/unit/*.test.ts",
    "package:release": "rm -rf release && mkdir -p release/kiel-univis-courses && npm run build && cp dist/index.mjs manifest.json release/kiel-univis-courses/ && cd release && zip -rq kiel-univis-courses.zip kiel-univis-courses"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}